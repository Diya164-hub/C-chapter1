#include <stdio.h>
#include <math.h>

int main(){
    int a = 6;
    printf("The value of this square is %f\n", pow(a, 2));
    // (a, 2): mean a to the power 2
    // Floting number ke liye %f use karna chahiye
    return 0;
}