//Chapter: ARRAYS

#include <stdio.h>

int main(){
    int marks[90]; // Reservve space to store 90 integers

    marks[0] = 48;
    marks[1] = 85;
    marks[2] = 67;
    // We can go all the way till 89 = 90 elements
    printf("Marks 0, marks 1 and marks 2 is %d %d %d", marks[0], marks[1], marks[2]);
    return 0;
}
 